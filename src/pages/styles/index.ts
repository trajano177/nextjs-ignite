import { createStitches } from '@stitches/react'

export const {
    config,
    styled,
    css,
    keyframes,
    getCssText,
    theme,
    createTheme
} = createStitches({
    theme: {
        colors: {
            rockeseat: '#n0257e6',
        }
    }
})